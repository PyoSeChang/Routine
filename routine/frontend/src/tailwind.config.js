/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{js,ts,jsx,tsx}", // ✅ src 폴더 내 모든 JS/TSX 파일에 적용
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}